name: Hello World
on:
  # push:
  #   branches:
  #     - main
  workflow_dispatch:
jobs:
  say-hello:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "👋 Hello from a GitHub Action Workflow!"
